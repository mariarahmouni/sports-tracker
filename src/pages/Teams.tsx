import NavBar from "../components/navigation-bar/NavBar";

const Teams = () => {
    return(
        <div>
            <NavBar />
            <h1>Hello, I am the Teams page.</h1>
        </div>

    );
}

export default Teams; 